import styled from "styled-components";

const LogoLayout = styled.div`
  margin-bottom: 40px;
`;

const Logo = () => {
  return (
    <LogoLayout >
      내밥몇칼로리?
    </LogoLayout>
  );
}

export default Logo;